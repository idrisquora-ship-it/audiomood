# dependencies
node_modules/

# Prefer npm + package-lock.json on CI/EAS so postinstall patches apply reliably
bun.lock

# expo
.expo/
dist/

# Generated native dirs (managed workflow + EAS prebuild — do not commit)
android/
ios/

# env (use EAS env / app.config extra for CI)
.env
.env.local

# tooling / IDE
.cursor/

# firebase admin SDK must never ship in-repo
**/audiomood-*-firebase-adminsdk*.json
